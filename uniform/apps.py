from django.apps import AppConfig


class UniformConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'uniform'
    verbose_name = 'Uniform — наложение формы на скин'
